import { forwardRef, type ButtonHTMLAttributes, type ReactNode } from "react"; import { componentClasses } from "@/src/design-system";
export type ButtonVariant = "primary" | "secondary" | "outline" | "ghost" | "danger" | "success";
const variants: Record<ButtonVariant, string> = { primary: "bg-brand text-on-brand hover:bg-brand-hover", secondary: "bg-[#374151] text-ink hover:bg-[#4B5563]", outline: "border border-edge-15 text-ink hover:bg-edge-5", ghost: "text-ink hover:bg-edge-5", danger: "bg-red-500 text-white hover:bg-red-600", success: "bg-brand-hover text-white hover:bg-[#15803D]" };
export interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> { variant?: ButtonVariant; loading?: boolean; leadingIcon?: ReactNode }
export const Button = forwardRef<HTMLButtonElement, ButtonProps>(function Button({ variant = "primary", loading = false, leadingIcon, disabled, className = "", children, ...props }, ref) { return <button ref={ref} disabled={disabled || loading} aria-busy={loading || undefined} className={`inline-flex min-h-11 items-center justify-center gap-2 rounded-lg px-4 text-sm font-semibold transition ${componentClasses.focus} ${variants[variant]} ${className}`} {...props}>{loading ? <span className="h-4 w-4 animate-spin rounded-full border-2 border-current border-r-transparent" aria-hidden="true"/> : leadingIcon}{children}</button>; });
export function IconButton({ label, children, ...props }: Omit<ButtonProps, "aria-label"> & { label: string }) { return <Button aria-label={label} title={label} className="min-w-11 px-2" {...props}>{children}</Button>; }
