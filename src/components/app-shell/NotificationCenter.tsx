"use client";
import { useTranslations } from "next-intl";
import type { AppNotification } from "./types";
export function NotificationCenter({ open, notifications, onClose }: { open:boolean; notifications:AppNotification[]; onClose:()=>void }) {
  const tShell = useTranslations("appShell");
  if(!open)return null; return <section id="notification-center" aria-labelledby="notifications-title" className="fixed right-3 top-16 z-40 max-h-[min(30rem,calc(100dvh-5rem))] w-[min(24rem,calc(100vw-1.5rem))] overflow-hidden rounded-2xl border border-white/10 bg-[#161A34] shadow-2xl"><header className="flex min-h-14 items-center justify-between border-b border-white/10 px-4"><h2 id="notifications-title" className="font-semibold text-[#E5E7EB]">{tShell("notifications")}</h2><button type="button" onClick={onClose} aria-label={tShell("closeNotifications")} className="min-h-11 min-w-11 rounded-lg text-[#E5E7EB] focus-visible:outline-2 focus-visible:outline-[#22C55E]">×</button></header><ul className="max-h-96 overflow-y-auto p-4">{notifications.length?notifications.map(item=><li key={item.id} className="border-b border-white/10 py-4"><div className="flex gap-3">{item.unread?<span className="mt-2 h-2 w-2 shrink-0 rounded-full bg-[#22C55E]" aria-label={tShell("unread")}/>:null}<div><h3 className="text-sm font-semibold text-[#E5E7EB]">{item.title}</h3><p className="mt-1 text-sm text-[#9CA3AF]">{item.description}</p><time className="mt-2 block text-xs text-[#6B7280]">{item.timestamp}</time></div></div></li>):<li className="p-6 text-center text-sm text-[#9CA3AF]">{tShell("noNotifications")}</li>}</ul></section>; }
