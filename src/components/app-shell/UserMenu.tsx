"use client";
import { useTranslations } from "next-intl";
import Link from "next/link"; import type { AppUserSummary } from "./types";
import { signOutAction } from "@/app/[locale]/auth/actions";
export function UserMenu({ user }: { user:AppUserSummary }) {
  const tShell = useTranslations("appShell");
  return <details className="relative"><summary aria-label={tShell("openUserMenu")} className="flex min-h-11 cursor-pointer list-none items-center gap-2 rounded-lg px-1 sm:px-2 focus-visible:outline-2 focus-visible:outline-brand-bright"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-brand/10 text-xs font-bold text-brand-bright">{user.initials}</span><span className="hidden text-left xl:block"><span className="block text-sm font-semibold text-ink">{user.name}</span><span className="block text-xs text-ink-muted">{user.email}</span></span></summary><div className="absolute right-0 z-30 mt-2 w-56 rounded-xl border border-edge-10 bg-surface p-2 shadow-xl">{[[tShell("profile"),"#"],[tShell("settings"),"/dashboard/settings"],[tShell("support"),"#"]].map(([label,href])=><Link key={label} href={href} className="flex min-h-11 items-center rounded-lg px-3 text-sm text-ink-soft hover:bg-edge-5 focus-visible:outline-2 focus-visible:outline-brand-bright">{label}</Link>)}<form action={signOutAction}><button type="submit" className="flex min-h-11 w-full items-center rounded-lg px-3 text-sm text-ink-soft hover:bg-edge-5 focus-visible:outline-2 focus-visible:outline-brand-bright">{tShell("logout")}</button></form></div></details>; }
