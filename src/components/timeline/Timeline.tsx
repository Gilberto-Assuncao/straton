import type { ReactNode } from "react";
export interface TimelineItem { id: string; title: string; description?: string; timestamp: string; icon?: ReactNode }
export function Timeline({ items, empty }: { items: TimelineItem[]; empty?: ReactNode }) { if (!items.length) return <>{empty ?? <p className="text-sm text-ink-muted">No activity yet.</p>}</>; return <ol className="relative border-l border-edge-10 pl-6">{items.map((item) => <li key={item.id} className="relative pb-6 last:pb-0"><span className="absolute -left-[1.95rem] flex h-4 w-4 items-center justify-center rounded-full bg-brand ring-4 ring-surface">{item.icon}</span><time className="text-xs text-ink-muted">{item.timestamp}</time><h3 className="mt-1 text-sm font-semibold text-ink">{item.title}</h3>{item.description ? <p className="mt-1 text-sm text-ink-muted">{item.description}</p> : null}</li>)}</ol>; }
