"use client";

import { useTranslations } from "next-intl";
import ApprovalStatusBadge from "@/components/timesheets/ApprovalStatusBadge";
import type { TimesheetEntry } from "@/lib/types/timesheet";

export default function TimesheetCard({ entry, onEdit }: { entry: TimesheetEntry; onEdit: (entry: TimesheetEntry) => void }) { const t = useTranslations("timesheets"); return <article className="rounded-2xl border border-white/10 bg-[#161A34] p-4"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="text-xs text-[#9CA3AF]">{entry.date}</p><h4 className="mt-1 truncate font-semibold text-[#E5E7EB]">{entry.project}</h4><p className="mt-1 text-sm text-[#9CA3AF]">{entry.task}</p></div><ApprovalStatusBadge status={entry.status} /></div><dl className="mt-5 grid grid-cols-3 gap-3"><div><dt className="text-xs text-[#6B7280]">{t("cardStart")}</dt><dd className="mt-1 text-sm font-medium text-[#E5E7EB]">{entry.startTime}</dd></div><div><dt className="text-xs text-[#6B7280]">{t("cardEnd")}</dt><dd className="mt-1 text-sm font-medium text-[#E5E7EB]">{entry.endTime}</dd></div><div><dt className="text-xs text-[#6B7280]">{t("cardTotal")}</dt><dd className="mt-1 text-sm font-semibold text-[#4ADE80]">{Math.floor(entry.workedMinutes / 60)}h {entry.workedMinutes % 60}m</dd></div></dl>{entry.canEdit ? <button type="button" onClick={() => onEdit(entry)} aria-haspopup="dialog" className="mt-5 min-h-11 w-full rounded-lg border border-white/15 text-sm font-semibold text-[#E5E7EB] hover:bg-white/5 focus-visible:outline-2 focus-visible:outline-[#22C55E]">{t("editEntry")}</button> : null}</article>; }
