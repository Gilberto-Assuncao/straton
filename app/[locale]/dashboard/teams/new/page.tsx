import type{Metadata}from"next";import{getTranslations}from"next-intl/server";import{notFound}from"next/navigation";import{DashboardCard}from"@/src/components/dashboard";import{getTeamWorkspace,TeamForm}from"@/src/features/teams";export const metadata:Metadata={title:"Create team"};export default async function NewTeamPage(){const[workspace,t]=await Promise.all([getTeamWorkspace(),getTranslations("teams")]);if(!workspace.permissions.includes("manage"))notFound();return <div className="mx-auto max-w-5xl space-y-6"><header><p className="text-sm font-semibold text-brand">{t("title")}</p><h1 className="mt-1 text-2xl font-bold sm:text-3xl">{t("createTeam")}</h1><p className="mt-2 text-sm text-ink-muted">{t("teamWillBelongTo",{company:workspace.companyName})}</p></header><DashboardCard title={t("teamInformation")} description={t("teamInformationHelp")}><TeamForm members={workspace.availableMembers}/></DashboardCard></div>}
