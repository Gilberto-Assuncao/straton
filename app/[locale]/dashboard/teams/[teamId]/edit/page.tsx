import type{Metadata}from"next";import{getTranslations}from"next-intl/server";import{notFound}from"next/navigation";import{DashboardCard}from"@/src/components/dashboard";import{getTeamDetails,TeamForm}from"@/src/features/teams";export const metadata:Metadata={title:"Edit team"};export default async function EditTeamPage({params}:{params:Promise<{teamId:string}>}){const{teamId}=await params;const[team,t]=await Promise.all([getTeamDetails(teamId),getTranslations("teams")]);if(!team||!team.permissions.includes("manage"))notFound();return <div className="mx-auto max-w-5xl space-y-6"><header><p className="text-sm font-semibold text-brand">{team.companyName}</p><h1 className="mt-1 text-2xl font-bold sm:text-3xl">{t("editNamed",{name:team.name})}</h1></header><DashboardCard title={t("teamSettings")}><TeamForm teamId={team.id} members={[...team.availableMembers,...team.members.map(member=>({membershipId:member.membershipId,name:member.name,email:member.email,jobTitle:member.jobTitle}))]} values={{name:team.name,description:team.description,status:team.status,color:team.color,icon:team.icon,leaderMembershipId:team.leaderMembershipId??"",memberIds:team.members.map(member=>member.membershipId)}}/></DashboardCard></div>}
